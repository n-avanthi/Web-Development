const g = 9.18;

const add = (x, y) => {x + y};
const sub = (x, y) => {x - y};
const mul = (x, y) => {x * y};

module.exports = {add, sub, mul};
module.exports = {g};

//g & add/sub cannot be in same module.exports